apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: api
spec:
  type: ClusterIP

  selector:
    app: api

  ports:
  - name: http
    port: 5678
    targetPort: 5678
    protocol: TCP