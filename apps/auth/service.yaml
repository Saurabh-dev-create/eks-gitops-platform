apiVersion: v1
kind: Service
metadata:
  name: auth
  labels:
    app: auth
spec:
  type: ClusterIP

  selector:
    app: auth

  ports:
  - name: http
    port: 5678
    targetPort: 5678
    protocol: TCP