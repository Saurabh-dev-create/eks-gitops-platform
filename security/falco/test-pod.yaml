apiVersion: v1
kind: Pod
metadata:
  name: falco-test

spec:
  containers:
  - name: shell
    image: busybox

    command:
      - sh
      - -c
      - sleep 3600

    resources:
      requests:
        cpu: 50m
        memory: 64Mi

      limits:
        cpu: 100m
        memory: 128Mi